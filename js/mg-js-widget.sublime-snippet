<snippet>
	<content><![CDATA[
define([
    'jquery',
${1:    'jquery/ui',}
${2:    'jquery-ui-modules/widget'}
], function(\$) {
    'use strict';

    \$.widget('${3:mage}.${4:mywidget}', {
        options: {
            optionOne: '',
            optionTwo: '',
        },

        /**
         * @private
         */
        _create: function() {
            console.log('widget created');
            console.log(this.element);
            console.log(this.options);
        },
    });

    return \$.${3:mage}.${4:mywidget};
});

]]></content>
	<!-- Optional: Set a tabTrigger to define how to trigger the snippet -->
	<tabTrigger>mg-js-widget</tabTrigger>
	<!-- Optional: Set a scope to limit where the snippet will trigger -->
	<!-- <scope>source.python</scope> -->
</snippet>
